<%= form_with(model: Job.new) do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= select(:job, :job_type, Job.job_types.collect { |t| [ t[0], t[1] ] }) %>

  <%= form.submit %>
<% end %>
